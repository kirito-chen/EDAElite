#pragma once

#include <iostream>
#include <map>
#include "object.h"
#include "arch.h"

void calculateTileRemain();
void FM();
void generateOutputFile(const std::string &filename);

// void initialPlacement();
void generateOutputFile(const std::string &filename);